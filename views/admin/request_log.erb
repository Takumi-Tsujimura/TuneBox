<h2>リクエストログ　(フォームID: <%= @form.form_key %>)</h2>

<form method="GET" action="/admin">
  <button>戻る</button>
</form>

<% if @requests.empty? %>
  <p>まだリクエストはありません。</p>
<% else %>
  <ul style="list-style-type: none; padding: 0;">
    <% @requests.each do |r| %>
      <li style="border: 1px solid #ccc; border-radius: 6px; padding: 10px; margin-bottom: 10px;">
        <strong>日時:</strong> <%= r.created_at.strftime("%Y-%m-%d %H:%M") %><br>
        
        <strong>曲名:</strong> <%= r.track_name %><br>
        <strong>アーティスト:</strong> <%= r.track_artists %><br>
        <strong>トラックID:</strong> <%= r.track_id %>
        
        <% if r.form.form_type == 'general' %>
          <strong>ニックネーム:</strong> <%= r.user_name %><br>
        <% elsif r.form.form_type == 'school' %>
          <strong>学年:</strong> <%= r.grade %><br>
          <strong>クラス:</strong> <%= r.class_name %><br>
          <strong>番号:</strong> <%= r.number %><br>
          <strong>名前:</strong> <%= r.user_name %><br>
        <% end %>

        <form method="POST" action="/forms/<%= @form.form_key %>/tracks/<%= r.track_id %>">
          <input type="hidden" name="_method" value="delete">
          <input type="hidden" name="form_key" value="<%= @form.form_key %>">
          <input type="hidden" name="track_id" value="<%= r.track_id %>">
          <button type="submit">削除</button>
        </form>
      </li>
    <% end %>
  </ul>
<% end %>
