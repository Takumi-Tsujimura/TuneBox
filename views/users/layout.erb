<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-SgOJa3DmI69IUzQ2PVdRZhwQ+dy64/BUtbMJw1MZ8t5HZApcHrRKUc4W0kG879m7" crossorigin="anonymous">
  <title>タイトル</title>
  <style>
    /* モーダルのスタイル */
    .modal {
      display: none;
      position: fixed;
      z-index: 1;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.5);
    }
    .modal-content {
      background-color: #fff;
      margin: 15% auto;
      padding: 20px;
      border: 1px solid #888;
      width: 80%;
    }
    .close {
      color: #aaa;
      float: right;
      font-size: 28px;
      font-weight: bold;
    }
    .close:hover,
    .close:focus {
      color: black;
      text-decoration: none;
      cursor: pointer;
    }
     ul {
      list-style: none; /* 黒丸を消す */
      padding: 0; /* 左側の余白をなくす（必要に応じて） */
      margin: 0;
    }
  </style>
</head>
<body>
  <div class="px-5">
    <div>
      <div>
        <a href="/form/<%= @form.form_key %>" style="color:black; text-decoration:none;"><h1><%= @form.form_name %></h1></a>
      </div>
      <div class="container" style="margin-top: 30px; margin-bottom: 30px; margin-left: 0; margin-right: 0; padding: 0;">
        <form method="GET" action="/search/<%= @form.form_key %>" class="d-flex align-items-center" style="max-width: 600px; margin-left: 0; margin-right: 0;">
          <input class="form-control me-2" type="search" name="keyword" id="keyword" value="<%= params[:keyword] %>" placeholder="キーワードを入力" style="flex: 1;">
          <button class="btn btn-primary" type="submit" style="white-space: nowrap;">検索</button>
        </form>
      </div>
    </div>
    <%= yield %>
  </div>
</body>
</html>